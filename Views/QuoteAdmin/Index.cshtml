@model IEnumerable<QuoteGeneratorAPI.Models.Quote>

<h1>Quotes Administration</h1>

@foreach (var quote in Model)
{
    <div>
        <h2>@quote.Author</h2>
        <p>@quote.QuoteText</p>
        <!-- Add more details and controls as needed -->
    </div>
}