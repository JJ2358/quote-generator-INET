@{
    ViewData["Title"] = "Quote Generator";
}

<h2>@ViewData["Title"]</h2>

<div id="quoteList">
    <!-- This section will be populated with quotes from the database -->
</div>

<button id="addQuote">Add Quote</button>

@section Scripts {
    <script>
        document.addEventListener('DOMContentLoaded', function() {
            // JavaScript to load quotes and handle add, update, delete actions
        });
    </script>
}
